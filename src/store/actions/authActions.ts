import {
  ILoginRequestActionType,
  ILoginSuccessActionType,
  ILoginFailureActionType,
} from "./types";
import { ThunkAction } from "redux-thunk";

// export const loginActionCreator = (data:{userName:string, password:string}): ThunkAction =>{

// }
